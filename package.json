{
  "name": "ambitiontransition.fr",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "sass:watch": "npm run sass -- -w ./stylesheets -r --source-map true",
    "dev:watch": "npm run sass:watch & npm run live-server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alban Petit",
  "license": "ISC",
  "dependencies": {
  }
}
